{
  "extends": "../tsconfig.base.json", // Inherit from the base config
  "compilerOptions": {
    "outDir": "dist", // Server-specific output directory
    "rootDir": "src", // Specify the root directory of source files
  },
  "include": ["src/**/*"], // Include source files relative to this directory
  "exclude": ["node_modules", "dist"], // Exclude folders relative to this directory
}
